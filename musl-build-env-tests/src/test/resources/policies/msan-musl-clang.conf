# Stand-in for the /etc/musl-clang.conf a derived MSan image would install.
MUSL_CLANG_SANITIZE=memory
MUSL_CLANG_COMPILE_FLAGS=(
    -fsanitize=memory
    -fPIE
)
MUSL_CLANG_LINK_FLAGS=(
    -fsanitize=memory
    -fPIE
    -pie
)
