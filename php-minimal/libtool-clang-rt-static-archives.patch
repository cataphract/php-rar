diff --git a/build/libtool.m4 b/build/libtool.m4
--- a/build/libtool.m4
+++ b/build/libtool.m4
@@ -3877,7 +3877,7 @@ if AC_TRY_EVAL(ac_compile); then
   for p in `eval $output_verbose_link_cmd`; do
     case $p in
 
-    -L* | -R* | -l*)
+    -L* | -R* | -l* | */libclang_rt.*.a)
        # Some compilers place space between "-{L,R}" and the path.
        # Remove the space.
        if test $p = "-L" \
